Patch from Laurent Bercot to allow multi-token CC tool.
diff --git a/configure b/configure
--- a/configure
+++ b/configure
@@ -79,7 +79,7 @@ cmdexists () {
 }
 
 trycc () {
-  test -z "$CC_AUTO" && cmdexists "$1" && CC_AUTO=$1
+  test -z "$CC_AUTO" && cmdexists "$1" && CC_AUTO="$*"
 }
 
 stripdir () {
